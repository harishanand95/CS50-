#include<stdio.h>

#include<cs50.h>
int main()
{
    int amt,i;
    double num,var;
    printf("Number:");
    for(int i=0;i<16;i++)
    {   if(isalpha(
        scanf("%d",&A[i]);
        
            
    num=var;
    amt=num%10;
    for(i=0;amt<1;i++)
     {   num=(int)num/10;amt=num%10;}
    int A[i];
    i-=1;
    for(;!(i==0);i--)
    {
        A[i]=var%10;
        var=(int)var/10;
    }  
    for(i=0;i<12;i++)
    printf("%d",A[i]);
    return 0;
}    
